# 项目简介｜片盒点检 MediaPack Audit

> 工作草案：由 Codex 协助整理。提交前请补入本人真实遇到的素材交付问题、实际试用记录和自己的技术取舍，并确认能够解释全部提交内容。

**一句话介绍：** 面向数字媒体素材交付的 MoonBit 命令行预检工具，把素材规格清单变成可重复运行、可分享的 HTML 检查报告。

## 背景与问题

数字媒体课程和小型内容项目经常需要交付海报、封面、社媒配图与界面资源。人工核对文件名、尺寸和大小容易遗漏；反复口头确认也难以留下统一记录。目标用户是需要把素材交给老师、客户或开发同伴的学生设计师与小型制作团队。

## 解决方案

用户提供一份 JSON 清单和一个素材目录。工具逐项检查清单文件是否存在、是否为普通文件、PNG 文件头尺寸是否符合要求、文件大小是否超限，并列出目录中未登记的 PNG。结果同时输出为终端摘要、独立 HTML 报告和 JSON 报告。示例素材包可在本机离线复现。

## MoonBit 的作用

项目主体逻辑和 CLI 使用 MoonBit 实现；使用 MoonBit async 文件 API 读取目录与文件头，使用 MoonBit JSON API 解析清单和生成结果。当前以 native target 运行，重点展示 MoonBit 在轻量命令行工具和数字内容工作流中的实际应用。

## 演示流程

运行 README 中的一条 `moon run` 命令，检查一张合格 PNG、清单中缺失的社媒图和一张多余 PNG；终端显示结果，并生成中文 HTML 页面与 JSON 文件。演示不依赖云服务或账号授权。

## MVP 范围与边界

首版只支持单层素材目录中的小写 `.png` 文件，并检查存在性、普通文件类型、IHDR 宽高、字节上限、重复清单项和额外文件。它不解码完整图片，不判断图像视觉质量、色彩空间、透明边缘或语义内容，也不修改素材。

## 阶段计划

1. 完成清单解析、PNG 头检查、HTML/JSON 报告与最小示例。
2. 补齐异常输入测试、清晰错误提示和 Windows/Linux 可复现步骤。
3. 邀请数字媒体同学用真实交付清单试用，依据反馈调整报告字段和演示内容。
4. 整理项目说明、录制端到端演示，并根据组织方对选题范围的反馈确认提交方向。

## 方向区分与待确认事项

本项目把范围限定为“数字媒体文件包规格验收”，不扩展为图像编辑、生成式创作、素材检索或通用资产管理。公开信息无法确认其他参赛者的选题，因此不能预先保证绝对不重叠；正式投入前应把这份简介发给活动组织者，请其确认方向是否合适。
